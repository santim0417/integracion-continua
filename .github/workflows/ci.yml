name: API Quality Gate
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm integracioncontinua
      - run: npx cypress run --spec "cypress/e2e/tc_pruebank.cy.js"